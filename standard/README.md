# Standard Model Package

Place the FP32 ONNX files and tokenizer files in this directory.

Required by `pipeline.json`:

- `text_encoder.onnx`
- `meanflow_step.onnx`
- `semanticvae_decode.onnx`
- `tokenizer/`
